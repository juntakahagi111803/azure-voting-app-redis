
git config --global user.name ""
git config --global user.email ""
git config init.defaultBranch main

git status
git add .\test.txt
git commit -m "test.txtの1回目保存を実行"

git log --oneline -n 2
git log -p

#コミットハッシュのみを選択して確認
git show f65dabe


git diff
#ステージにgit addしたものを確認
git diff --staged


#直近のcommit時のメッセージを修正する場合 --amendを利用する！
git commit --amend -m "04を追加" 


